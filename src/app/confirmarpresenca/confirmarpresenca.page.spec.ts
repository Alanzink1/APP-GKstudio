import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ConfirmarpresencaPage } from './confirmarpresenca.page';

describe('ConfirmarpresencaPage', () => {
  let component: ConfirmarpresencaPage;
  let fixture: ComponentFixture<ConfirmarpresencaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ConfirmarpresencaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
